{
  "name": "poc-blockcerts-chapi-wallet",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "compile": "npm run compile:worker && npm run compile:index",
    "compile:index": "rollup -c rollup.iife.config.js -w",
    "compile:worker": "rollup -c rollup.iife.worker.config.js -w",
    "compile:worker:get": "rollup -c rollup.iife.worker.get.config.js -w"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "credential-handler-polyfill": "^3.0.0",
    "web-credential-handler": "^2.0.0"
  },
  "devDependencies": {
    "@rollup/plugin-typescript": "^8.4.0",
    "rollup": "^2.78.1",
    "rollup-plugin-serve": "^2.0.1",
    "tslib": "^2.4.0",
    "typescript": "^4.8.2"
  }
}
